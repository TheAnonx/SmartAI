<Window x:Class="SmartAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🧠 Smart AI - Inteligência que Aprende" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Window.Resources>
        <Style x:Key="MessageBubbleUser" TargetType="Border">
            <Setter Property="Background" Value="#5865F2"/>
            <Setter Property="CornerRadius" Value="15,15,0,15"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="50,5,10,5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="MessageBubbleAI" TargetType="Border">
            <Setter Property="Background" Value="#2B2D42"/>
            <Setter Property="CornerRadius" Value="15,15,15,0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10,5,50,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <Style x:Key="MessageText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#5865F2" Padding="20">
            <StackPanel>
                <TextBlock Text="🧠 Smart AI" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtStatus"
                           Text="Inteligência Artificial que Aprende com Você" 
                           FontSize="12" 
                           Foreground="#B9BBBE"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Chat Area -->
        <Border Grid.Row="1" Background="#36393F" Margin="10">
            <ScrollViewer x:Name="scrollChat" 
                          VerticalScrollBarVisibility="Auto"
                          Padding="10">
                <StackPanel x:Name="chatPanel"/>
            </ScrollViewer>
        </Border>

        <!-- Stats Bar -->
        <Border Grid.Row="2" Background="#2B2D42" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="📚 Conhecimento: " Foreground="#B9BBBE" FontSize="12"/>
                    <TextBlock x:Name="txtKnowledgeCount" Text="0 itens" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="💬 Conversas: " Foreground="#B9BBBE" FontSize="12"/>
                    <TextBlock x:Name="txtConversationCount" Text="0" Foreground="White" FontSize="12" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="✓ Aprendi: " Foreground="#B9BBBE" FontSize="12"/>
                    <TextBlock x:Name="txtLearnedCount" Text="0 coisas" Foreground="#57F287" FontSize="12" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="3" Background="#40444B" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtInput" 
                         Grid.Column="0"
                         Background="#2B2D42"
                         Foreground="White"
                         FontSize="14"
                         Padding="15,10"
                         BorderThickness="0"
                         VerticalContentAlignment="Center"
                         KeyDown="TxtInput_KeyDown">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ScrollViewer x:Name="PART_ContentHost"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button x:Name="btnSend" 
                        Grid.Column="1"
                        Content="📤 Enviar"
                        Background="#5865F2"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="Bold"
                        Padding="20,10"
                        Margin="10,0,0,0"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BtnSend_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4752C4"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>